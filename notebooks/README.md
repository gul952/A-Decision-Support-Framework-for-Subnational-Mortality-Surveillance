# Notebooks

Exploratory analysis notebooks go here. Note: notebooks are for
exploration only -- anything that produces a committed output (a
processed data file, a figure, a model) should live as a proper script
under scripts/, not only in a notebook, so scripts/run_pipeline.sh can
reproduce the entire project end-to-end without manually re-running
notebook cells.
